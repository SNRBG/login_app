# Login App

This is a simple login application built using Flask and MongoDB. It supports user login with encrypted passwords.

## How to run

1. Clone the repository.
2. Install dependencies: `pip install -r requirements.txt`
3. Run the Flask app: `python app.py`
